const executeOperation = (operand1, operand2, operator) => {
  switch(operator){
    case '+':
      return operand1 + operand2;
    case '-':
      return operand1 - operand2;
    case '*':
      return operand1 * operand2;
    case '/':
      return operand1 / operand2;
    default:
      return null;
  }
};

const 

const findResult = (operationSet) => {
  // const { operandsList, operatorsList } = operationSet;
  // operatorsList.forEach((operator, index) => {
  //   if(operator) == ''
  // });
};

export default findResult;
